pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                bat "java -version"
                bat "git --version"
                echo "fin"
            }
        }
    }
}
